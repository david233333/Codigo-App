<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="GreenText" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/editText_hint</item>
        <item name="android:textSize">@dimen/size_text</item>
        <item name="android:textColorPrimary">@color/editText_hint</item>
        <item name="colorAccent">@color/editText_hint</item>
        <item name="colorControlNormal">@color/editText_hint</item>
        <item name="colorControlActivated">@color/editText_hint</item>


    </style>

    <style name="GreenTextb" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/colorblack</item>
        <item name="android:textSize">@dimen/size_text</item>
        <item name="android:textColorPrimary">@color/colorblack</item>
        <item name="colorAccent">@color/colorblack</item>
        <item name="colorControlNormal">@color/colorblack</item>
        <item name="colorControlActivated">@color/colorblack</item>


    </style>

</resources>
